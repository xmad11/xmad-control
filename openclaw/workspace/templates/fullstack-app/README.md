# Fullstack App Template

## Purpose
Complete starter template for full-stack applications with frontend and backend.

## Structure (Placeholder)
- Frontend (React + Vite)
- Backend (Express + TypeScript)
- Shared types package
- Monorepo setup with workspaces
- Docker configuration

## Usage
Generated via `create-factory-project` CLI:

```bash
pnpm create:project --template fullstack-app
```

## Next Steps
- Create frontend template (Vite + React)
- Create backend template (Express)
- Add shared types package
- Configure workspace
- Add Dockerfile
- Add docker-compose
